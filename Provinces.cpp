
#include <iostream.h>
#include <stdlib.h>
#include <iostream.h>

int main()
{
    return 0;
}

/*class Solution {
public:
    int findCircleNum(vector<vector<int>>& isConnected) {
        int proviceNum = 0;
        int size = isConnected.length();
        for(int i = 0; i < size; i++)
        {
            
        }
    }
};

class DisjoinSet {
public:
    DisjoinSet(int sz)
    {
        
    }
    int findCircleNum(vector<vector<int>>& isConnected) {
        int proviceNum = 0;
        int size = isConnected.length();
        for(int i = 0; i < size; i++)
        {
            
        }
    }
};*/
